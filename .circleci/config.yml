version: 2.1

jobs:
  build:
    docker:
      - image: ryangraham/credz:0.0.2
    steps:
      - checkout
      - run:
          name: Build
          command: make build
      - run:
          name: Run
          command: ./build/credz --version
